# dependências
node_modules/

# séries de preço baixadas — 136 MB, reproduzíveis com `npm run download`.
# ANCORADO na raiz (/data/): sem a barra inicial, o padrão "data/" também
# casava com src/data/ em qualquer profundidade, e isso deixou store.ts e
# stocks.ts — código-fonte, não dado — fora do git desde sempre.
/data/

# estado de execução: posições abertas, diários, logs.
# ANCORADO na raiz (/pasta/): sem a barra inicial, "research/" casava com
# src/research/ e deixou scout.ts (código-fonte real) fora do git desde
# sempre, sem ninguém perceber — o mesmo bug que /data/ já tinha tido.
/spread/
/vigilancia/
/momentum/
/preenchimento/
/pares/
/renda/
/paper/
/state/
/models/
/research/

# relatórios gerados
/reports/

# saídas temporárias de execução
*.log
*.txt
!requirements.txt

# segredos — nunca versionar
.env
.env.*
*.key
credentials.json
