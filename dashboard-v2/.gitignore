# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright — artefatos transitórios de execução (traces, screenshots de
# falha, relatório HTML). NUNCA versionados. As baselines de regressão visual
# (e2e/*-snapshots/) NÃO entram aqui de propósito — essas SIM são versionadas.
test-results/
e2e-report/
playwright/.cache/
.last-run.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
